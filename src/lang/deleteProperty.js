import defn from './defn'

const deleteProperty = defn('lang.deleteProperty', {
  description: 'Delete the given Property from a Propertied object',
  since: 'v0.2.0'
})

export default deleteProperty
