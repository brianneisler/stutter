import Symbol from '../util/js/Symbol'

/**
 * The `Symbol.for('@@protocols')` defines a set of protocols that a value has implemented
 *
 * @private
 * @type {Symbol}
 * @since v0.1.0
 * @category lang.constants
 */
const SYMBOL_PROTOCOLS = Symbol.for('@@protocols')

export default SYMBOL_PROTOCOLS
