import Symbol from '../util/js/Symbol'

/**
 * The `Symbol.for('@@meta')` defines a set of meta data on an object
 *
 * @private
 * @type {Symbol}
 * @since v0.1.0
 * @category lang.constants
 */
const SYMBOL_META = Symbol.for('@@meta')

export default SYMBOL_META
