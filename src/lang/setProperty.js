import defn from './defn'

const setProperty = defn('lang.setProperty', {
  description: 'Set the value at a given Property for the given Propertied collection',
  since: 'v0.2.0'
})

export default setProperty
