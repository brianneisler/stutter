import { Repeat as ImmutableRepeat } from 'immutable'

/**
 *
 * @function
 * @since v0.1.0
 * @category lang
 * @param {*} value
 * @returns {Repeat} A new `Immutable.Repeat`
 * @example
 */
const Repeat = ImmutableRepeat

export default Repeat
