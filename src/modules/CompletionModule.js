// This module should provide a `stutter completion` command
// that can be used to ouput the completion script and appended
// to any config file

// Installation: stutter completion >> ~/.bashrc  (or ~/.zshrc)
// Or, maybe: stutter completion > /usr/local/etc/bash_completion.d/npm
